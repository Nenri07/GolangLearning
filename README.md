Golang Series with Hitesh Chaudry
1. download the golang and save the path in env veriables of window
2. fmt.Println(”something”) for print
3. golang auto import the stuff.
4. importing package main on every file maybe
5. func main(){  } .it has a starting function for compilation which is always named main.
6. lexer is the tech which auto check the collon and in context hwere use or not used it auto manage that.
7. go help will give access to the documentation.
8. go mod init “anything” initialized the mod file 
9. go run main.go run the app

## Veriables in Golang

1.we use var nameofveriable type = value

1. we use fmt.Printf(”someting ”, value) for as string interpolation
2. %T \n in used for the type of veriable
3. we have bool type of true or false
4. unit with bytes for limit like uint8 means 2^8 which is 256 and its limit is 0-255
5. float32 and float64 with different numb length after dot
6. Alieases are the default values we use 
7. if we just intialize the variable and dont asign anything then it default is 0 or empty 
8. implicit type 
    1. var something = “Umer”
    2. no var style we use wolerace operator := 
    3. Wolerace operators are accessable and just work inside a method not work as global.
    4. const if first letter is capital then its public const
9. comma ok syntax
